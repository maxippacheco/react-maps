import { BtnLocation, MapView, ReactLogo, SearchBar } from "../components"

export const HomeScreen = () => {
	return (
		<>
			<MapView />
			<BtnLocation />
			<ReactLogo />
			<SearchBar />
		</>
	)
}
